from __future__ import annotations


def select_k_from_noise_stub(noise_amplitude_r: float) -> int:
    """Placeholder for later-phase r-based truncation logic."""
    raise NotImplementedError(
        "r-based cutoff is intentionally not part of the core pipeline in this phase."
    )
