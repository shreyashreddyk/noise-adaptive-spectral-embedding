name: synthetic_bandwidth_sweep
output_root: results
base_config: swiss_roll_stability.yaml
seeds: [101, 202]
cases:
  - name: narrow_bandwidths
    overrides:
      graph:
        epsilon: 0.8
        epsilon_grid: [0.5, 0.8, 1.1]
  - name: wide_bandwidths
    overrides:
      graph:
        epsilon: 1.2
        epsilon_grid: [0.6, 1.2, 2.4, 3.0]
