name: ambiguous_gap_suite
output_root: results
base_config: ambiguous_gap_baseline.yaml
seeds: [7, 8, 9]
cases:
  - name: eigengap_mode
    overrides:
      cutoff:
        method: eigengap
      data:
        noise_std: 0.12
  - name: stability_mode
    overrides:
      cutoff:
        method: bandwidth_stability
      data:
        noise_std: 0.12
